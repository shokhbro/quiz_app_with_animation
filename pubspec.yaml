name: quiz_app_with_animation
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  firebase_core: ^3.1.1
  flutter:
    sdk: flutter
  cloud_firestore: ^5.0.2
  provider: ^6.1.2
  lottie: ^3.1.2
  gap: ^3.0.1
  firebase_auth: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/lotties/
  
  fonts:
    - family: Lato
      fonts:
        - asset: assets/fonts/Lato-Bold.ttf
    
  
